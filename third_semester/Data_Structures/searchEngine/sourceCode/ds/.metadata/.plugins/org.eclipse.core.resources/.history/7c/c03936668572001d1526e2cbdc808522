package searchEngine;

import java.util.Iterator;

public class searchEngineTest {

	public static void main(String[] args) {
		//searchEngine test = new searchEngine();
		Dictionary<Integer, String> a = new Dictionary<Integer, String>();
		a.add(1, "test");
		a.add(1, "testestets");
		
	}
	 void displayWordss(Dictionary<Integer, String> dataBase) {
		Iterator<Integer> keyIterator = dataBase.getKeyIterator();
		Iterator<String> valueIterator = dataBase.getValueIterator();
		while (keyIterator.hasNext()) {
			System.out.println("Key: " + keyIterator.next() + " Value: " + valueIterator.next());
		}
	}
}
